const char* gnu_ctype_h_name = "ctype.h";

const char* gnu_ctype_h_globals[] = {
    // The GNU C Library <ctype.h> header declares character classification and conversion functions,
    // which may also be defined as macros, but it does NOT declare or define any global variables.
    NULL
};

// Types declared in gnu/ctype.h
const char* gnu_ctype_h_types[] = {
    // None; this header primarily provides macros and function declarations for character classification and conversion
    NULL
};

// Macros declared in gnu/ctype.h
const char* gnu_ctype_h_macros[] = {
    "isalnum",    // Macro or function to test for alphanumeric character
    "isalpha",    // Macro or function to test for alphabetic character
    "iscntrl",    // Macro or function to test for control character
    "isdigit",    // Macro or function to test for decimal digit
    "isgraph",    // Macro or function to test for visible character (not space)
    "islower",    // Macro or function to test for lowercase letter
    "isprint",    // Macro or function to test for printable character (including space)
    "ispunct",    // Macro or function to test for punctuation character
    "isspace",    // Macro or function to test for whitespace character
    "isupper",    // Macro or function to test for uppercase letter
    "isxdigit",   // Macro or function to test for hexadecimal digit
    "tolower",    // Macro or function to convert uppercase letter to lowercase
    "toupper",    // Macro or function to convert lowercase letter to uppercase
    NULL
};

// Functions declared in gnu/ctype.h
const char* gnu_ctype_h_functions[] = {
    "isalnum",    // int isalnum(int c);
    "isalpha",    // int isalpha(int c);
    "iscntrl",    // int iscntrl(int c);
    "isdigit",    // int isdigit(int c);
    "isgraph",    // int isgraph(int c);
    "islower",    // int islower(int c);
    "isprint",    // int isprint(int c);
    "ispunct",    // int ispunct(int c);
    "isspace",    // int isspace(int c);
    "isupper",    // int isupper(int c);
    "isxdigit",   // int isxdigit(int c);
    "tolower",    // int tolower(int c);
    "toupper",    // int toupper(int c);
    NULL
};

