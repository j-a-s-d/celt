const char* assert_h_name = "assert.h";

const char* assert_h_globals[] = {
    // <assert.h> declares the assert macro for runtime condition checking,
    // but it does NOT declare or define any global variables with external linkage.
    NULL
};

// Types declared in assert.h
const char* assert_h_types[] = {
    // assert.h does not declare new types
    NULL
};

// Macros declared in assert.h
const char* assert_h_macros[] = {
    "assert",
    "NDEBUG",
    NULL
};

// Functions declared in assert.h
const char* assert_h_functions[] = {
    // assert.h declares no functions, only macros
    NULL
};

