const char* iso646_h_name = "iso646.h";

const char* iso646_h_globals[] = {
    // <iso646.h> defines eleven macros as alternative spellings for C operators,
    // but it does NOT define any global variables with external linkage.
    NULL
};

// Types declared in iso646.h
const char* iso646_h_types[] = {
    // iso646.h does not declare any types
    NULL
};

// Macros declared in iso646.h
const char* iso646_h_macros[] = {
    "and",       // alternative token for &&
    "and_eq",    // alternative token for &=
    "bitand",    // alternative token for &
    "bitor",     // alternative token for |
    "compl",     // alternative token for ~
    "not",       // alternative token for !
    "not_eq",    // alternative token for !=
    "or",        // alternative token for ||
    "or_eq",     // alternative token for |=
    "xor",       // alternative token for ^
    "xor_eq",    // alternative token for ^=
    NULL
};

// Functions declared in iso646.h
const char* iso646_h_functions[] = {
    // iso646.h declares no functions
    NULL
};

